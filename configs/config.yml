server:
  HTTP:
    host: "0.0.0.0"
    port: "8080"
  grpc:
    host: "0.0.0.0"
    port: "8081"

storage:
  type: "redis"
  redis:
    host: "localhost"
    port: "6379"
    db: 0
    expires: 5m

logger:
  level: "INFO"
  file: "logs/app.log"
